#include <stdio.h>

/**
 * main - printing decimals without char data type
 *
 * Return: 0
 */

int main(void)
{
		int er;

		for (er = 48; er <= 57; er++)
				putchar(er);
		putchar(10);
		return (0);
}
